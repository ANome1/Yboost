# ⚠️ NE PAS UTILISER EN LOCAL
# Ce fichier sert de RÉFÉRENCE pour configurer Scalingo

# ==============================================
# VARIABLES D'ENVIRONNEMENT PRODUCTION SCALINGO
# ==============================================

# IMPORTANT : Ces variables doivent être configurées dans le dashboard Scalingo
# Ne jamais commiter ce fichier avec des vraies valeurs !

# Mode production (obligatoire)
NODE_ENV=production

# Base de données (fournie automatiquement par l'addon PostgreSQL Scalingo)
# Ne PAS définir manuellement - Scalingo la génère
# DATABASE_URL=postgres://user:password@host:port/database

# Secret de session (GÉNÉRER UNE CLÉ ALÉATOIRE SÉCURISÉE !)
# Utiliser : openssl rand -base64 32
# Exemple : SESSION_SECRET=votre_clé_secrète_aléatoire_de_32_caractères_minimum
SESSION_SECRET=GENERATE_A_RANDOM_SECRET_HERE

# Niveau de logs (info pour production, debug pour troubleshooting)
LOG_LEVEL=info

# ==============================================
# INSTRUCTIONS DE DÉPLOIEMENT SCALINGO
# ==============================================

# 1. Créer l'application Scalingo (si pas déjà fait)
#    scalingo create mon-app-tah-les-fous

# 2. Ajouter l'addon PostgreSQL
#    scalingo --app mon-app-tah-les-fous addons-add postgresql postgresql-starter-512

# 3. Définir les variables d'environnement
#    scalingo --app mon-app-tah-les-fous env-set NODE_ENV=production
#    scalingo --app mon-app-tah-les-fous env-set SESSION_SECRET="$(openssl rand -base64 32)"
#    scalingo --app mon-app-tah-les-fous env-set LOG_LEVEL=info

# 4. Déployer manuellement
#    git push scalingo main

# 5. Vérifier les logs
#    scalingo --app mon-app-tah-les-fous logs --lines 100

# ==============================================
# VÉRIFICATION POST-DÉPLOIEMENT
# ==============================================

# ✅ DATABASE_URL doit apparaître automatiquement (fournie par l'addon PostgreSQL)
# ✅ SESSION_SECRET doit être une clé aléatoire longue (pas celle de l'exemple !)
# ✅ NODE_ENV doit être "production"
# ✅ LOG_LEVEL doit être "info" (ou "debug" temporairement pour troubleshooting)

# ==============================================
# SÉCURITÉ
# ==============================================

# ⚠️ NE JAMAIS :
#   - Commiter ce fichier avec de vraies valeurs
#   - Utiliser le même SESSION_SECRET en local et en production
#   - Mettre NODE_ENV=production en local
#   - Partager le SESSION_SECRET publiquement
