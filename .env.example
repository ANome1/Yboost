# Configuration de la base de données PostgreSQL

# En développement local (installer PostgreSQL et créer la base)
DATABASE_URL=postgresql://localhost/yboost_dev

# En production Scalingo (automatiquement fourni par l'addon)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Port du serveur (optionnel, par défaut 3000)
# PORT=3000
